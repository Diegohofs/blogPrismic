import { prismicImage, prismicText } from "../../types/general"; 

export interface prismicPostType {
    title?: prismicText[];
    main_content?: [];
    blog_post_date?: string;
    image?: prismicImage;
}